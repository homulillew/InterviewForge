"""Small authored offline baseline. Never presented as inferred project facts."""
from dataclasses import dataclass


@dataclass(frozen=True)
class Topic:
    title: str
    aliases: tuple[str, ...]
    mechanism: str
    decision: str
    failure: str
    validation: str
    alternative: str
    prerequisite: str
    prerequisite_explanation: str
    questions: tuple[str, ...]
    rubric: tuple[str, ...]


TOPICS = {
    "redis": Topic(
        "Redis Lua 并发库存扣减", ("redis", "lua", "超卖", "库存"),
        "读库存、判断和扣减若分成多个请求，会出现交错执行。把检查和扣减放入同一个 Redis Lua 脚本，"
        "可以阻止其他命令在脚本执行期间插入；这种原子执行不等于出错回滚，也不等于跨数据库事务。",
        "比较冲突率、业务事务边界和运维成本。数据库条件更新更贴近持久化事务；Redis Lua 适合短小、"
        "同实例内的原子操作，但需要单独处理与订单数据库的一致性。",
        "脚本过长会阻塞其他请求；运行时错误不回滚先前写入；故障切换可能丢失尚未复制的写；"
        "请求超时后重试可能重复扣减；集群多 key 操作需要满足槽位约束。",
        "并发提交超过初始库存的请求，检查库存非负且成功数与扣减数一致；注入超时重试和节点故障，"
        "检查幂等性及订单对账，再报告吞吐量、P99、客户端数和测试环境。",
        "数据库乐观锁 / 条件 UPDATE；分布式锁；MQ 串行消费",
        "Race condition 与原子性边界",
        "两个请求都读到同一份旧值再分别写回，会破坏业务不变量。同步必须覆盖完整的读—检查—写。"
        "原子性、隔离性和持久性是不同保证，不能由单条命令的原子性推导跨系统一致性。",
        ("为什么 Redis + Lua 能解决并发库存扣减的原子性问题？",
         "为什么这里选择 Redis Lua 而不是数据库乐观锁，决策依据是什么？",
         "如何证明并发扣减没有超卖，并可靠测量吞吐和 P99？",
         "Redis 执行后客户端超时，如何避免重试导致重复扣减？",
         "库存检查和扣减分成两个请求，为什么会发生 race condition？",
         "流量放大十倍形成 hot key 后，你会如何调整设计？"),
        ("原子", "重试", "边界", "验证"),
    ),
    "rag": Topic(
        "RAG 两阶段检索与 reranker", ("rag", "rerank", "检索", "向量", "search"),
        "第一阶段以较低成本找候选，重排器在候选内部比较 query 与文档的相关性。bi-encoder 可以预计算文档向量，"
        "cross-encoder 联合编码 query/document 通常更贵；不能仅凭名称断定具体模型结构。重排无法找回未召回的文档。",
        "先判断瓶颈是召回缺失、排序错误还是生成问题。增加候选数可能提高召回上限，同时增加重排延迟和成本；"
        "需要结合质量指标与延迟预算选择 top_k，不能假定越大越好。",
        "第一阶段漏召回时 reranker 无法补救；候选数增加导致尾延迟和成本上升；离线标签偏差、"
        "数据泄漏和分布漂移会使离线提升不能迁移到真实查询。",
        "建立独立查询集、文档相关性标签和固定切分；比较有无 reranker 的 Recall@K、MRR、NDCG@K、"
        "P99 与成本，并对 top_k 做消融。固定其他变量，保留原始结果和运行配置。",
        "BM25；dense retrieval；hybrid retrieval；只改 embedding 或 chunking",
        "Recall@K、MRR 与 NDCG",
        "Recall@K 衡量相关文档覆盖；MRR 关注首个相关结果的位置；NDCG 同时考虑分级相关性和位置折扣。"
        "排序指标需要真实相关性标签，不能用检索耗时或生成文本长度替代。",
        ("为什么 first-stage retrieval 不够，什么错误值得引入 reranker？",
         "如何衡量 reranker 的真实提升，避免评测数据泄漏？",
         "top_k 从 20 增至 100，会如何权衡 recall、precision、latency 和重排成本？",
         "如果相关文档没有进入候选集，为什么重排无法补救，如何排查？",
         "为什么 Recall@K、MRR 和 NDCG 可能给出不同结论？",
         "查询量增长十倍时，如何在质量与延迟预算内扩展检索服务？"),
        ("召回", "候选", "评测", "延迟"),
    ),
    "service": Topic(
        "服务可靠性与请求处理", ("service", "api", "服务", "接口", "熔断", "http"),
        "请求可能在网络、应用和存储任一层失败。超时只说明客户端未得到结果，不代表服务端没有执行。"
        "重试需要结合幂等键和持久化约束；限流、超时、隔离与熔断解决的是不同问题。",
        "先定义错误预算和业务不变量，再选择超时、有限重试或异步处理；用复杂组件前说明单体或数据库事务为何不够。",
        "无界重试可能放大故障；跨资源写入可能部分成功；连接池耗尽和慢依赖可能造成级联超时。",
        "测试正常、重复、超时和部分失败路径，观察成功率、错误分布和 P99；"
        "负载测试必须说明并发模型、持续时间与资源限制。",
        "同步事务；幂等异步任务；有限重试；限流与熔断",
        "幂等性与至少一次执行",
        "网络超时使调用方无法判断操作是否已提交。以业务唯一键把重复请求映射到同一结果，"
        "并把去重检查与业务写入放在同一事务边界，才有机会避免检查和写入间的竞争。",
        ("你如何定义这个服务需要守住的业务不变量？",
         "为什么选择当前请求处理方式，而不是异步任务或数据库事务？",
         "如何验证服务在超时重试下仍然满足正确性要求？",
         "依赖变慢并耗尽连接池时，你如何定位并限制故障扩散？",
         "为什么 HTTP 重试不天然具备业务幂等性？",
         "流量扩大十倍后，如何判断瓶颈在 CPU、连接池还是存储？"),
        ("超时", "幂等", "边界", "验证"),
    ),
}


def topic_for(text: str) -> str:
    lower = text.lower()
    for key in ("redis", "rag"):
        if any(a in lower for a in TOPICS[key].aliases):
            return key
    return "service"
